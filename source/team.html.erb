<div class="container">
  <ul class="list-inline">

    <% data.flats.each do |owner, flat| %>
      <li>
        <%= image_tag flat.owner_picture, class: "team-pictures" %>
        <h3 class="text-center"> <%= flat.name %> </h3>
      </li>
    <% end %>
  </ul>
</div>
